import '../core/animation_clip.dart';

class AnimationEngine {
  Future<void> execute(AnimationClip clip) async {
    // Placeholder: GPU / AI animation engine later
    await Future.delayed(const Duration(milliseconds: 16));
  }
}
