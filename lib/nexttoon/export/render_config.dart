class RenderConfig {
  final int width;
  final int height;
  final int fps;
  final String format; // mp4, webm

  RenderConfig({
    required this.width,
    required this.height,
    required this.fps,
    required this.format,
  });
}