name: Test NextToon Voices

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run test_voices
        run: |
          cd server/ai/nexttoon
          python test_voices.py
